
import 'package:flutter/material.dart';

class CameraSwitchWidgets extends StatelessWidget {
  const CameraSwitchWidgets({super.key});

  @override
  Widget build(BuildContext context) {
    return IconButton(
      icon: const Icon(Icons.switch_camera_sharp, color: Colors.white),
      onPressed: () {},
    );
  }
}
